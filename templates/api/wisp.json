{ 
  "response": "{{ response }}",
  "result": {
    "wisp": {
      "name": "{{ wisp.name }}",
      "dynamic_image_url": "{{ wisp.dynamic_image_url }}",
      "token": "{{ wisp.token }}",
      "id": "{{ wisp.key.id() }}"
    }
  }
}